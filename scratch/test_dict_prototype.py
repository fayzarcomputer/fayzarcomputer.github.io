import json
import re
import sys

sys.stdout.reconfigure(encoding='utf-8')

# Let's inspect some of the unique Bengali phrases extracted
with open('scratch/full_untranslated_breakdown.py', 'r', encoding='utf-8') as f:
    pass

print("Prototyping dictionary structure...")
